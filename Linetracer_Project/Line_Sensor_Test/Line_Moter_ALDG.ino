/*void setup() 
{
  Serial.begin(9600);
  pinMode(pin, OUTPUT); //모터 왼쪽
  pinMode(pin, OUTPUT); //모터 왼쪽
  pinMode(pin, OUTPUT); //모터 오른쪽
  pinMode(pin, OUTPUT); //모터 오른쪽
  
}
  void loop()
  {
    
    ////직진일떄
    ////아날로그로 했을떄.
    analogWrite(pin, 255);//// 모터 오른쪽
    analogWrite(pin, 0);
    analogWrite(EN A pin, ///Motor_speed);  
    analogWrite(pin, 255);///모터 왼쪽
    analogWrite(pin, 0);
    analogWrite(EN B pin, ///Motor_speed);  
    ////디지털로 했을때.
    digitalWrite(pin, HIGH);   // 모터 오른쪽 설정
    digitalWrite(pin, LOW);  
    analogWrite(EN A pin, ///Motor_speed);  
    digitalWrite(pin, HIGH);  // 모터 왼쪽 설정
    digitalWrite(pin, LOW);
    analogWrite(EN B pin, ///Motor_speed);
  }*/
